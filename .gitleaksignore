# Local dev SSH host key. Gitignored, never committed, and verified on
# 2026-09-06 to be absent from history and from unreachable objects; its
# fingerprint matches nothing serving on play.ssharcade.dev. `gitleaks dir`
# ignores .gitignore, so it flags the file on every scan — this stops that
# recurring at the Phase 6 visibility flip.
#
# gitleaks matches FINGERPRINTS here, not paths: "<file>:<rule>:<line>",
# relative to the directory gitleaks was pointed at. Scan from the repo root.
var/dev/ssh_host_key:private-key:1
